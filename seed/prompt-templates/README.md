# prompt-templates seed

这里存放 v0.1 的最小模板集：

- synopsis -> story
- story -> screenplay
- screenplay -> narrative scenes / dialogue
- render segment planning
- cut / layout / render prompt generation

模板内容必须与 `Qwen-compatible` 结构化 JSON 输出策略一致。
